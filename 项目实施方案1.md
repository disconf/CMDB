可以，技术路线先固定如下，并按“先底座、再核心能力、最后AI”的顺序实施。

## 一、确定的技术栈

| 领域 | 技术选型 |
|---|---|
| 前端 | Vue 3 + TypeScript + Vite + Pinia + Ant Design Vue |
| 模块化前端 | qiankun微前端 + 动态菜单/路由 |
| API网关 | Apache APISIX |
| 后端 | Golang，推荐Go 1.24+ |
| Web框架 | Gin |
| 内部通信 | gRPC |
| 外部接口 | RESTful API |
| 数据库 | PostgreSQL高可用集群 |
| 缓存 | Redis Cluster |
| 消息总线 | Kafka 3节点以上 |
| 图数据库 | Neo4j集群 |
| 搜索 | OpenSearch，用于CMDB检索和审计搜索 |
| 指标监控 | Prometheus + Thanos + Alertmanager |
| 日志 | Loki + Grafana Alloy |
| 链路追踪 | OpenTelemetry + Tempo |
| 可视化 | Grafana |
| 任务/流程引擎 | Temporal |
| 任务执行 | 自研Go Agent + Ansible Runner |
| 认证 | Keycloak，对接LDAP/AD/OIDC |
| 密钥管理 | HashiCorp Vault |
| 对象存储 | MinIO |
| 容器平台 | Kubernetes |
| 部署 | Helm + Argo CD |
| 镜像仓库 | Harbor |
| CI | GitLab CI |
| 拓扑展示 | AntV G6 |
| AI知识库向量库 | PostgreSQL + pgvector |
| AI模型接入 | 独立LLM Gateway，兼容OpenAI协议 |

说明：Keycloak、Kafka、Neo4j等基础组件本身不一定使用Go，但平台自行开发的业务微服务统一使用Golang。

---

# 二、完整实施路线

## 阶段0：需求收敛和架构规范

### 工作内容

1. 确定管理规模：

   - 第一阶段支持1,000～5,000台服务器。
   - 支持多个机房、云平台和Kubernetes集群。
   - 支持不少于100个项目组。
   - 支持不少于500个并发任务。
   - 明确指标和日志保存周期。

2. 确定统一术语：

   - 项目、应用、服务、实例。
   - 资源、资产、配置项CI。
   - 项目组、负责人、环境、区域。
   - 告警、事件、工单、任务、发布。

3. 编写架构约束：

   - 微服务禁止直接访问其他服务数据库。
   - 所有资源使用全局唯一`resource_id`。
   - 同步调用使用REST/gRPC。
   - 异步通知使用Kafka。
   - 跨服务操作使用Saga/补偿机制。
   - 所有操作携带用户、项目组和追踪ID。

### 产出物

- 产品需求说明书。
- 总体架构设计书。
- 微服务拆分清单。
- 数据字典。
- API规范。
- Kafka事件规范。
- 安全与审计规范。
- 容量规划报告。

### 验收标准

- 业务边界没有明显重叠。
- CMDB资源模型通过评审。
- 明确数据所有权。
- 明确第一期与后续范围。

---

## 阶段1：基础设施和开发框架

### 工作内容

1. 建设开发、测试、预生产、生产Kubernetes环境。
2. 部署基础组件：

   - APISIX。
   - Keycloak。
   - PostgreSQL。
   - Redis Cluster。
   - Kafka。
   - Neo4j。
   - MinIO。
   - Vault。
   - Harbor。
   - GitLab Runner。
   - Argo CD。

3. 建立Go微服务统一模板：

```text
cmd/                 服务启动入口
internal/api/        HTTP/gRPC接口
internal/application 应用逻辑
internal/domain/     领域模型
internal/repository/ 数据访问接口
internal/infrastructure 基础设施实现
pkg/                 可复用公共库
api/                 OpenAPI/Proto定义
migrations/          数据库迁移
deploy/              Helm配置
tests/               集成测试
```

4. 建设公共能力：

   - 配置管理。
   - 日志规范。
   - 错误码。
   - OpenTelemetry。
   - 数据库访问。
   - Redis访问。
   - Kafka生产和消费。
   - 幂等处理。
   - 分布式锁。
   - 健康检查。
   - 优雅关闭。
   - 限流和熔断。

### 产出物

- Go微服务脚手架。
- Helm Chart模板。
- CI/CD流水线模板。
- API和事件规范。
- 公共SDK。
- 基础组件部署清单。

---

## 阶段2：统一认证、权限和系统管理

### 微服务

```text
iam-service             用户和身份映射
permission-service      RBAC和数据权限
organization-service    部门、项目组、人员
module-service          模块注册和启停
menu-service            菜单、路由、按钮权限
audit-service           操作审计
gateway-bff             前端聚合接口
```

### 权限模型

采用RBAC + 数据范围：

```text
用户
 → 角色
 → 功能权限
 → 项目组数据范围
 → 资源标签范围
```

例如，用户拥有“服务器查看”权限，但只能查看所属项目组的服务器。

### 模块管理

系统管理员可在页面启用：

- CMDB。
- 监控中心。
- 任务管理。
- 工单管理。
- CI/CD。
- AI运维。
- 排查工具。

启用模块时完成：

1. 检查依赖模块。
2. 开启API路由。
3. 注册前端入口。
4. 注册菜单。
5. 注册权限点。
6. 检查服务健康状态。
7. 记录审计日志。

部署和升级仍由Helm + Argo CD完成，页面不能直接随意启动容器。

---

## 阶段3：CMDB核心

这是整个平台最重要的阶段。

### 微服务

```text
cmdb-model-service       CI模型和属性定义
cmdb-resource-service    资产实例管理
cmdb-relation-service    资产关系管理
cmdb-location-service    机房、模块组、机柜、U位
cmdb-tag-service         标签、项目组和环境
cmdb-search-service      资产检索
cmdb-import-service      Excel/API批量导入
cmdb-event-service       资产变更事件
cmdb-history-service     变更历史和版本
```

### 数据范围

- 园区、机房、模块组、机柜、U位。
- 物理服务器。
- 虚拟机。
- VMware/OpenStack资源。
- 云平台、区域、可用区、云主机。
- Kubernetes集群、Node、Namespace、Workload、Pod。
- 网络设备、端口、VLAN、网卡、IP。
- 项目、应用、服务、实例。
- 数据库、中间件、负载均衡。

### 数据存储原则

- PostgreSQL：资产主数据。
- Neo4j：资产关系和拓扑查询。
- OpenSearch：全文检索。
- Redis Cluster：热点缓存。
- Kafka：资产变化通知。

数据写入流程：

```text
请求写入PostgreSQL
 → 提交Outbox事件
 → 发布Kafka
 → 更新Neo4j关系投影
 → 更新OpenSearch索引
 → 删除Redis缓存
```

不能在业务请求里同步写四套存储，否则容易产生数据不一致。

### 验收重点

- 资产唯一性。
- 数据来源可追踪。
- 资产变更有历史版本。
- Neo4j故障不影响CMDB主数据写入。
- 同一事件重复消费不会产生脏数据。
- 项目组数据隔离有效。

---

## 阶段4：自动发现与Agent系统

### 微服务

```text
discovery-service        发现任务管理
collector-service        云、VMware、K8s、SNMP采集
agent-manager-service    Agent注册和管理
agent-gateway-service    Agent长连接入口
reconcile-service        数据对账和冲突处理
```

### Agent能力

- 主机硬件和操作系统信息采集。
- 软件包和服务发现。
- 端口和进程信息。
- 心跳和在线状态。
- 任务执行。
- 文件上传下载。
- 监控Exporter状态检查。

### 接入范围

- Linux Agent。
- Windows Agent。
- Kubernetes API。
- VMware vCenter。
- OpenStack。
- 阿里云、腾讯云、华为云、AWS。
- SNMP、LLDP、Redfish/IPMI。

### 安全要求

- Agent使用双向TLS。
- 每个Agent拥有独立身份。
- 不保存长期明文凭据。
- Agent只能执行签名任务。
- 支持凭据吊销和Agent隔离。
- Agent升级采用灰度发布。

---

## 阶段5：拓扑中心

### 拓扑类型

- 机房物理拓扑。
- 机柜设备拓扑。
- 网络连接拓扑。
- 虚拟化拓扑。
- Kubernetes拓扑。
- 应用服务拓扑。
- 业务依赖拓扑。
- 告警影响拓扑。

### 实现步骤

1. 从CMDB关系事件生成Neo4j节点和边。
2. 建立关系类型和方向规范。
3. 实现上下游查询。
4. 实现最短路径和影响范围查询。
5. 接入K8s、网络和服务调用关系。
6. 使用AntV G6展示拓扑。
7. 支持按项目组、环境、资源类型过滤。
8. 将告警状态叠加到拓扑节点。

### 注意事项

Neo4j是查询投影，不是CMDB资产主数据源。拓扑可重建，这一点非常关键。

---

## 阶段6：监控中心

### 微服务

```text
monitor-target-service     监控目标管理
monitor-template-service   监控模板
monitor-rule-service       告警规则
alert-center-service       告警中心
alert-process-service      聚合、抑制、降噪
notification-service       通知服务
dashboard-service          仪表盘管理
```

### 与CMDB联动

```text
CMDB资产变化
 → Kafka
 → 监控目标服务
 → 根据资产类型和项目组匹配模板
 → 生成Prometheus目标
 → 自动添加监控
```

### 监控体系

- Prometheus按机房或项目组分片。
- Thanos统一查询和长期存储。
- Alertmanager高可用部署。
- Loki保存平台和主机日志。
- Tempo保存调用链。
- Grafana统一展示。
- OpenTelemetry负责平台微服务可观测性。

### 告警处理

```text
原始告警
 → 去重
 → 聚合
 → 抑制
 → 关联CMDB
 → 查询拓扑影响
 → 匹配负责人
 → 通知或生成工单
```

---

## 阶段7：任务管理

### 微服务

```text
job-template-service      作业模板
job-scheduler-service     调度管理
job-execution-service     执行控制
job-worker-service        分布式Worker
script-service            脚本管理
credential-service        凭据引用
job-log-service           执行日志
```

### 执行流程

```text
创建任务
 → 权限检查
 → 风险检查
 → 审批
 → Temporal工作流
 → Kafka分发
 → 区域Worker
 → Agent/SSH/WinRM/K8s API执行
 → 实时日志
 → 汇总结果
```

### 必须支持

- 批量执行。
- 分批执行。
- 并发限制。
- 暂停、取消和重试。
- 定时任务。
- 失败中止。
- 自动回滚。
- 高危命令拦截。
- 完整审计。
- WebSocket/SSE实时日志。

---

## 阶段8：工单管理

### 工单类型

- 事件工单。
- 变更工单。
- 发布工单。
- 权限申请。
- 资源申请。
- 故障工单。
- 巡检工单。

### 实现方式

Temporal负责流程状态和可靠执行，前端使用bpmn-js或自定义流程设计器展示流程。

工单可以关联：

- CMDB资产。
- 告警。
- 项目组。
- 执行任务。
- 发布记录。
- 故障复盘。

高风险任务必须由工单审批后生成一次性执行授权。

---

## 阶段9：CI/CD服务管理

### 集成范围

- GitLab。
- GitLab CI。
- Harbor。
- Helm。
- Argo CD。
- Kubernetes。

### 流程

```text
Git提交
 → GitLab CI构建
 → 测试和安全扫描
 → 推送Harbor
 → 更新Helm版本
 → 发布审批
 → Argo CD部署
 → 健康检查
 → 更新CMDB实例
 → 自动纳入监控
```

平台不需要重新实现GitLab CI或Argo CD，而是提供统一管理、审批、状态展示、审计和CMDB关联。

---

## 阶段10：排查工具中心

提供标准化工具：

- Ping、TCP端口、DNS。
- Traceroute和路由查询。
- HTTP接口测试。
- SSL证书检查。
- Linux进程和端口查询。
- Kubernetes Pod、Event、日志和资源检查。
- 数据库连通性检查。
- Prometheus指标查询。
- Loki日志查询。
- 拓扑路径检查。

所有工具通过任务平台执行，不能让浏览器直接访问目标服务器。

---

## 阶段11：AI运维

### 第一阶段只做分析

- 告警摘要。
- 日志分析。
- 故障关联。
- CMDB资产查询。
- 拓扑影响分析。
- 工单总结。
- Runbook推荐。
- 故障报告生成。

### 第二阶段做受控执行

```text
用户问题
 → AI生成排查计划
 → 调用只读工具
 → 给出原因和建议
 → 用户确认
 → 工单审批
 → 任务平台执行
```

### AI安全边界

- AI不直接连接服务器。
- AI不保存生产凭据。
- 工具必须白名单注册。
- 查询受项目组权限限制。
- 高风险操作必须审批。
- 模型输入输出必须审计和脱敏。
- 不允许把密钥、密码、Token发送给外部模型。

---

## 阶段12：安全、压测和生产验收

### 验证内容

- 5,000台资产同步压测。
- 1,000个Agent同时心跳。
- 500个并发任务。
- Kafka积压恢复。
- PostgreSQL主从切换。
- Redis Cluster节点故障。
- Neo4j不可用时CMDB正常写入。
- Prometheus和Loki高负载测试。
- Agent断网重连。
- 权限越权测试。
- 高危任务审批绕过测试。
- 灾备恢复演练。

### 上线条件

- 核心服务至少两个副本。
- 数据库具备备份和恢复验证。
- RPO、RTO明确。
- 监控和告警覆盖平台自身。
- 安全扫描无高危问题。
- 审计记录不可被普通管理员删除。
- 发布与回滚流程经过演练。

---

# 三、实施时使用的 Skills

这些Skill当前环境中已经具备，不需要额外安装。

| 阶段 | 使用Skill | 用途 |
|---|---|---|
| 总体规划 | `source-command-plan` | 需求重述、风险分析、分阶段计划 |
| 架构设计 | `backend-patterns` | 微服务边界、数据访问、消息和缓存模式 |
| API设计 | `api-design` | REST资源、分页、错误响应、版本管理 |
| Go开发 | `golang-patterns` | Go项目结构、并发、接口和错误处理 |
| 编码规范 | `coding-standards` | 命名、可读性和工程规范 |
| 错误处理 | `error-handling` | 跨服务错误码、重试和降级 |
| 测试驱动 | `tdd-workflow` | 先测试后实现 |
| Go测试 | `golang-testing` | 单元、集成、基准和模糊测试 |
| 构建修复 | `source-command-go-build` | 修复编译、静态检查和依赖问题 |
| 前端架构 | `vue-patterns` | Vue 3组件和状态管理 |
| 前端设计 | `frontend-design-direction` | 门户、CMDB、拓扑和监控界面方向 |
| 前端工程 | `frontend-patterns` | 权限路由、数据状态和性能 |
| UI完善 | `make-interfaces-feel-better` | 交互细节和易用性 |
| 浏览器验收 | `playwright`、`e2e-testing` | 端到端业务流程验证 |
| 安全审查 | `security-best-practices` | API、认证、Agent、密钥和AI安全 |
| AI测试 | `ai-regression-testing` | AI分析结果回归测试 |
| AI评估 | `eval-harness` | 告警分析、故障判断的质量评估 |
| MCP工具 | `mcp-server-patterns` | 如果AI工具层采用MCP协议 |
| 生产审计 | `production-audit` | 上线前生产就绪检查 |
| 完整验证 | `verification-loop` | 构建、测试、安全和功能综合验证 |
| 文档同步 | `source-command-update-docs` | 根据API和配置更新文档 |
| 架构导览 | `code-tour` | 为后续开发和运维人员生成代码导览 |

建议实际开发时每一个业务阶段都采用：

```text
source-command-plan
 → api-design / backend-patterns
 → golang-patterns
 → tdd-workflow
 → golang-testing
 → security-best-practices
 → verification-loop
```

目前按规划技能要求停在“方案确认”节点，不创建项目、不写代码。下一步应先开展阶段0和阶段1；需要你明确确认后再进入实施。