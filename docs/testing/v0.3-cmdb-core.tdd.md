# V0.3 CMDB 核心 TDD 证据

## 用户旅程

- 运维人员可以按照 CI 类型浏览资产。
- 运维人员可以按名称、编号、IP、状态和项目组筛选资产。
- 运维人员可以查看资产基础信息、扩展属性、标签和关系。
- 未登录用户不能读取 CMDB 数据。

## RED 证据

| 目标 | RED 结果 |
|---|---|
| CMDB 领域服务 | `NewService`、`AssetQuery` 和 `ErrNotFound` 不存在，编译失败 |
| 前端查询模型 | `cmdbModel` 不存在，测试加载失败 |

## GREEN 证据

| 保证 | 测试/验证 | 结果 |
|---|---|---|
| 搜索、类型和状态组合筛选正确 | `internal/cmdb/service_test.go` | PASS |
| 非法分页参数被标准化 | `internal/cmdb/service_test.go` | PASS |
| 资产详情包含属性和关系 | `internal/cmdb/service_test.go` | PASS |
| 未知资产返回 not found | `internal/cmdb/service_test.go` | PASS |
| 模型数量与状态汇总一致 | `internal/cmdb/service_test.go` | PASS |
| CMDB API 要求登录并正确返回目录 | `internal/httpapi/server_test.go` | PASS |
| 前端生成紧凑、正确编码的查询参数 | `cmdbModel.test.ts` | PASS |
| 状态和类型转换为中文展示值 | `cmdbModel.test.ts` | PASS |
| 资产台账和详情抽屉真实渲染 | Chromium 实际操作 | PASS |
| 浏览器控制台 | Playwright console | 0 错误，0 警告 |

## 覆盖率

- `internal/cmdb`：93.8%
- `internal/httpapi`：87.4%
- `internal/auth`：85.0%
- `internal/dashboard`：100.0%
- 前端：8 项单元测试全部通过

## 当前边界

- 当前资产数据保存在内存，后端重启后恢复为固定演示数据。
- 新增、编辑和导入按钮暂未实现写入操作。
- 项目组权限目前控制功能入口；行级数据隔离将在接入数据库后实现。
